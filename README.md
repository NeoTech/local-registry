Small bash script that needs to be run as root on your linux machine and a small docker-compose script
that will start a local registry with the necessary TLS certificates for your local IP but with no auth.
